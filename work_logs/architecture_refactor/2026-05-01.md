# Architecture Refactor Work Log

## 2026-05-01 23:21:36 +08:00

- Files changed:
  - `patchevent/models/backbone.py`
  - `patchevent/models/hybrid_event_decoder.py`
  - `patchevent/losses/matching.py`
  - `patchevent/models/factory.py`
  - `patchevent/models/__init__.py`
  - `patchevent/phase2/model.py`
  - `patchevent/phase2/train.py`
  - `patchevent/training/engine.py`
  - `patchevent/training/eval_loop.py`
  - `patchevent/phase2/collect_ablation_v2_results.py`
  - `patchevent/paper_data/extract_all_results.py`
- Architecture intent:
  - Created the new end-to-end `PatchBackboneEncoder` without Phase-1 heads or checkpoint dependency.
  - Added `HybridEventDecoder`, where DETR-style event queries produce set proposals and a causal event refinement block models event order internally.
  - Promoted Hungarian matching to the main hybrid loss path with dual localization/intensity heads.
  - Kept legacy checkpoint-based AR/DETR code available only through explicit `legacy_*` paths; the default training path is `hybrid`.
  - Removed Phase-1 encoder metric collection from the main result aggregation path.
- Local execution:
  - No training, evaluation, or GPU workload was run locally.
  - Only code editing and static checks are allowed on this machine.
- Remote validation required:
  - Run syntax/import checks, one-batch smoke training, and real experiments on the remote VM.

## 2026-05-01 23:34:00 +08:00

- Files changed:
  - `patchevent/phase2/train.py`
- Architecture intent:
  - Kept `encoder_mode=frozen` as the legacy default so old checkpoint-based paths remain compatible.
  - Hybrid training still ignores `encoder_mode` and uses the new end-to-end backbone.
- Local execution:
  - Re-ran static Python compilation only for `patchevent/phase2/train.py`.
- Remote validation required:
  - Confirm legacy checkpoint scripts only if old results need to be reproduced.
